# Default values for the chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1

image:
  sample:
    repository: 172.16.9.49:5000/sample
    tag: "latest"
  tomcat:
    repository: tomcat
    tag: "9-jdk8-openjdk-slim"
  pullPolicy: IfNotPresent
  pullSecrets: []

deploy:
  directory: /usr/local/tomcat/webapps

service:
  name: http
  type: LoadBalancer
  externalPort: 81
  internalPort: 8080

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  path: /
  hosts:
    - chart-example.local
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

#readinessProbe:
#  path: "/sample"
#  initialDelaySeconds: 60
#  periodSeconds: 30
#  failureThreshold: 6
#livenessProbe:
#  path: "/sample"
#  initialDelaySeconds: 60
#  periodSeconds: 30

resources: {}
#  limits:
#    cpu: 100m
#    memory: 256Mi
#  requests:
#    cpu: 100m
#    memory: 256Mi

nodeSelector: {}

tolerations: []

affinity: {}
